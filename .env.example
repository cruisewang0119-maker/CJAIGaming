# Required: Anthropic API key for Claude chat + event classification
ANTHROPIC_API_KEY=

# Required: Mapbox public access token for map rendering
NEXT_PUBLIC_MAPBOX_TOKEN=

# Optional: Reddit API credentials for live event data (falls back to mock if empty)
REDDIT_CLIENT_ID=
REDDIT_CLIENT_SECRET=
